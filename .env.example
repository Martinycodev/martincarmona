# Entorno: development | production
APP_ENV=production

# Ruta base: vacío si estás en el dominio raíz (ej: midominio.com)
# Usa /martincarmona si estás en un subdirectorio (XAMPP local)
APP_BASE_PATH=

# Base de datos
DB_HOST=localhost
DB_USER=tu_usuario_db
DB_PASS=tu_password_db
DB_NAME=tu_nombre_db
